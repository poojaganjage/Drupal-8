api: '2'
core: 8.x
# Download the Rms install profile and recursively build all its dependencies:
projects:
  rms:
    type: profile
    download:
      type: git
      branch: 8.x-1.x
includes:
  - drupal-org-core.make.yml
